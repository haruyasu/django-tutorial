{
  "name": "django",
  "version": "1.0.0",
  "private": true,
  "description": "Django Tutorial",
  "main": "index.js",
  "scripts": {
    "init": "gitbook init",
    "start": "gitbook serve",
    "build": "gitbook build",
    "publish": "gh-pages -d _book -m 'Auto publish book'"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "gitbook-cli": "^2.3.0"
  }
}
